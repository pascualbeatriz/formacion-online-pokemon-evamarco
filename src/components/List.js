import React from 'react';
import PropTypes from 'prop-types';

const List = props => {
  const {item} = props;
  return(
    <div className="card">

    </div>
  );
}
List.propTypes = {
  
}
export default List;